version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: install cmake
          command: sudo apt-get install cmake

      - run:
          name: run build
          command:  mkdir build && cd build && cmake .. && make
